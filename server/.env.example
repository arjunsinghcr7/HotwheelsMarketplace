# Copy to server/.env and fill in. server/.env is gitignored — never commit real keys.

# Anthropic API key for the AI assistant (format: sk-ant-...).
# If unset, the assistant runs in catalog-aware offline mode.
ANTHROPIC_API_KEY=

# Optional: override the API port (defaults to 5001).
# PORT=5001
